# 复制到仓库根目录并命名为 .release.env（该文件已被 git 忽略，勿提交）。
# 也可用 export 直接注入环境变量，两者等价；文件里的值不覆盖已有的 export。

# --- Apple 公证（必填）---
APPLE_ID=fengzhe1983@gmail.com
APPLE_APP_SPECIFIC_PASSWORD=xxxx-xxxx-xxxx-xxxx
APPLE_TEAM_ID=Q85U66X3JF

# --- 服务器与发布位置（必填）---
SERVER=root@114.55.15.112
SSH_KEY=/Users/zhefeng/.ssh/mareo-0.pem
REMOTE_DMG_DIR=/var/www/mareo-site/downloads
REMOTE_WEB_DIR=/var/www/mareo-site
SITE_URL=https://mareo.cn

# --- 可选 ---
# ELECTRON_MIRROR=https://npmmirror.com/mirrors/electron/   # 默认已指向该镜像
